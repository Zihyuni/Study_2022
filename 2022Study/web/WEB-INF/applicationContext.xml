<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- 스프링 애플리케이션 전반에 걸쳐 모든 구성요소의 제어 작업을 담당하는 IoC 엔진-->

    <!-- bean factory와 마찬가지로 bean객체를 생성하고 관리한 기능을 가진다.
        트랜잭션 관리, 메시지 기반의 다국어 처리, AOP 처리등등 DI(Dependency Injection)
        과 Ioc(Inverse of Conversion) 외에도 많은 부분을 지원

    1.web Application 최상단위에 위치하고 있는 context
    2.Spring에서 ApplicationContext란 BeanFactory를 상속받고 있는 Context
    3.Spring에서 root-context.xml, applicationContext.xml 파일은
    ApplicationContext 생성 시 필요한 설정정보를 담은 파일
    -->
    <!-- bean:스프링에서 스프링이 제어권을 가지고 직접 생성하고 의존관계를 부여하는 오브젝트-->
</beans>